package serhii.hrynkevych.forty;

public class Forty {
    //40) Can we access static members of outer class inside a member inner class?
    //40) Можем ли мы получить доступ к статическим членам внешнего класса внутри внутреннего класса-члена?

    //answer
    //Yes, we can access both static and non-static members of outer class inside a member inner class.
    //Да, мы можем получить доступ как к статическим, так и к нестатическим членам внешнего класса внутри внутреннего класса-члена.
}
